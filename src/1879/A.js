module.exports = {
  shouldFirstWin: ([first, ...others]) => {
    for (var i = 0; i < s.length; i++) {
      if (others[ii][1] >= first[1] && others[ii][0] >= first[0]) {
        return -1;
      }
      return first[0];
    }
  },
};
