const { getMaxValue } = require('./B');

describe('1825B. LuoTianyi and the Table', () => {
  it.each`
    n    | num  | m    | arr                                                                                          | result
    ${1} | ${2} | ${2} | ${[1, 3, 1, 4]}                                                                              | ${9}
    ${2} | ${2} | ${2} | ${[-1, -1, -1, -1]}                                                                          | ${0}
    ${3} | ${2} | ${3} | ${[7, 8, 9, -3, 10, 8]}                                                                      | ${64}
    ${4} | ${3} | ${2} | ${[4, 8, -3, 0, -7, 1]}                                                                      | ${71}
    ${5} | ${4} | ${3} | ${[-32030, 59554, 16854, -85927, 68060, -64460, -79547, 90932, 85063, 82703, -12001, 38762]} | ${1933711}
  `('Base test: $n', ({ num, m, arr, result }) => {
    expect(getMaxValue(num, m, arr)).toBe(result);
  });
});
