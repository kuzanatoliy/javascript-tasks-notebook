const { getMap } = require('./B');

describe('1841B. Keep it Beautiful', () => {
  it.each`
    n    | arr                                                                       | result
    ${1} | ${[3, 7, 7, 9, 2, 4, 6, 3, 4]}                                            | ${'111110010'}
    ${2} | ${[1, 1, 1, 1, 1]}                                                        | ${'11111'}
    ${3} | ${[3, 2, 1, 2, 3]}                                                        | ${'11011'}
    ${4} | ${[0, 4, 15, 18, 4, 10, 12, 8, 13, 8, 15, 0, 14, 12, 10, 12, 10, 14, 13]} | ${'1111000000010000000'}
  `('Base test: $n', ({ arr, result }) => {
    expect(getMap(arr)).toBe(result);
  });
});
